#Use the output of the previous command to find the name and IP address of the device that makes the link between you (local network) and the outside world

First line of the output is the gateway, but the second is nat(Network address translation). NAT happens at router, that's our link to outside world and it's IP is 10.60.1.1
